"use strict" // treat all JS code as newer version
// alert(10 + 8) // we're using nodeJS, not browser

console.log(10 + 8)

// number
// bigint
// string
// boolean
// null
// undefined // undefined is different from null value.
// symbol
// object

let a = 18
let b = "string"
let c = true
let d = 27572798279827985
let e
let f = null

console.table([typeof(null), typeof(undefined), typeof(a), typeof(b), typeof(c), typeof(d), typeof(e), typeof(f)])

